#include <iostream>
using namespace std;
#include <cmath>
#include <vector>
#include <iomanip>

int main()
{
    int r,c;
    int arr[r][c];
    for (int i = 0; i < r; i++)
    {
        
        for (int j = 0; j < c; j++)
        {
            cin >> arr[i][j];
        }
    }
    int x , y;
}